export class Constants {
    public static DEFAULT_POOL_SIZE: number = 10
    public static EPSILON: number = Number.MIN_VALUE
    public static DEG_TO_RAD: number = Math.PI / 180
    public static RAD_TO_DEG: number = 180 / Math.PI
    public static UINT_MAX: number = new Uint32Array([-1])[0]
}
